import TableFeatureLengthSelect from './'
import { render } from '../../utils/test'

describe('TableFeatureLengthSelect', () => {
    test('Should render without crashing', async () => {
        render (<TableFeatureLengthSelect />)
    })
})