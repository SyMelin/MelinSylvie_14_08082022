import SortButtonsContainer from './'
import { render } from '../../utils/test'

describe('SortButtonsContainer', () => {
    test.skip('Should render without crashing', async () => {
        render (<SortButtonsContainer />)
    })
})