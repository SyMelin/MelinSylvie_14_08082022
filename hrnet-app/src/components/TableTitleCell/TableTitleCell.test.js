import TableTitleCell from './'
import { render } from '../../utils/test'

describe('TableTitleCell', () => {
    test('Should render without crashing', async () => {
        render (<TableTitleCell />)
    })
})