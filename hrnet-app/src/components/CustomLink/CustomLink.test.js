import CustomLink from './'
import { render } from '../../utils/test'

describe('CustomLink', () => {
    test.skip('Should render without crashing', async () => {
        render (<CustomLink />)
    })
})