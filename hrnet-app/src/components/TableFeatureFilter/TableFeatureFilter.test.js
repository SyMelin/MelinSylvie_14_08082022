import TableFeatureFilter from './'
import { render } from '../../utils/test'

describe('TableFeatureFilter', () => {
    test('Should render without crashing', async () => {
        render (<TableFeatureFilter />)
    })
})